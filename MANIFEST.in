# Keep local-only and secret-bearing files out of the source distribution.
# (The wheel only ships the skribe package, but be explicit for the sdist.)
exclude .env .envrc .cursorrules
global-exclude .env .envrc
recursive-exclude .claude *
prune .claude
